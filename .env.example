# Copy to .env. The key stays on the Node server; never use a VITE_ prefix.
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-flash
PORT=5178
HOST=127.0.0.1
# Optional absolute data path. Default: .local-data/state.json in this project.
# DATA_FILE=/absolute/path/state.json
