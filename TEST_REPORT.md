# 本地验收记录

日期：2026-09-15。环境：macOS / Node.js 26.5 / Chrome，独立临时浏览器。使用服务 http://localhost:5178；UI/HMR、真实API、生产检查分别在5188、5189、5190使用临时数据，不污染用户内容。

## 已实际通过

- Vite 生产构建成功；npm 依赖安装审计：0 个已知漏洞（安装时）。
- 单元/API共13项：12项通过，1项因主服务已配置真实密钥而跳过离线API格式测试，避免无意消费。包含密钥/数据文件不可读取、非法输入/外域拒绝、磁盘重启恢复、并发版本冲突、保存失败重试、冲突备份、HTTP确认丢失恢复，以及22个新增角色ID/社区范围校验。
- 完整浏览器演示流程：欢迎登录 → 默认会话 → 你好 → emoji加载 → 分段回复 → 展开旁白。
- 回复建议只填入草稿，不自动发送。
- 发送第二条消息 → 每日记忆；记忆图片灯箱；聊天记录搜索与消息定位。
- 回复模式选择；发现角色 → 角色主页 → 新会话；不同角色会话隔离。
- 发布日常；刷新后本地内容仍在。
- 320 / 375 / 402 / 414 / 768px 下没有页面水平溢出；发现页原始图片加载成功。
- 浏览器运行上述流程无未捕获 JavaScript 异常。
- CSS 热替换、React Fast Refresh 均实际触发；页面不整页重载，当前聊天与草稿保留。
- 查看了欢迎页、聊天页、发现页、记忆页的实际渲染截图，对照原稿核对素材和布局。
- 真实 DeepSeek `deepseek-flash` 端到端成功：发送自定义「桂花乌龙、项目后有点累」内容后得到相关回复、可展开角色旁白和记忆；另外调用真实「帮我回复」，建议只进入草稿。完整通过的live测试使用2次真实请求；早一次测试也成功调用2次，但在测试上传步骤定位错误时停止，修正测试后重跑通过。
- 四个底栏逐一核验：当前项fill、其他三项line；玻璃导航贯穿四页，输入栏覆盖聊天滚动内容并带backdrop-filter。记忆所有日期emoji包含蓝色圆形底图。
- 文字 + 图片发布、点赞后，确认后端接口含对应数据；全新浏览器上下文（无原localStorage）能恢复动态、聊天和记忆。
- 生产构建实际启动，写入隔离数据后停止并重启Node进程，数据仍在；前端构建不含配置的API密钥。

截图位于 `test-results/`，可用 `npm run test:ui` 重建。

## 新增页面验收（Figma 9–14）

- 读取实际Figma设计上下文，保留原头像裁切、透明图片处理、SceneLayers、聊天逻辑；新增素材使用Figma原始导出文件。
- `test:community` 已通过：社区两个分栏独立显示；点赞/评论/发布落盘；私人动态不出现在广场；关闭评论再打开保留当前会话草稿；无缓存新浏览器恢复已发内容。
- 22个星点均对应唯一角色ID；选点放大→标签显现→同名人物资料→新增会话；取消后等待超出完整时间线不会误跳页。键盘焦点保持在过渡层，减少动态模式可完成流程。
- 使用「项目工作完成后很累，想休息」验证本地匹配将心安排在首位；没有将示例记忆作为兴趣来源，也没有向模型发送这些匹配输入。
- 柔化首页光晕：测试实际检查soft-bloom动画并等待播放结束卸载，截图 `v2-welcome-soft.png`；各新增页面截图 `v2-square/friends/comment/planet/traits/profile.png` 已目视核对。
- 320/375/402/430/768宽度无页面横向溢出；原有`test:ui`、`test:hmr`、生产构建及`test:production`复测通过。
- 新角色chun另做1次真实DeepSeek调用：HTTP200、mode=live、返回2段回复且包含「春念久」，不写入用户聊天；广场示例互动不调用模型。

边界：广场为本地社区预览，其他用户内容/计数为示例；Hi使用预设人物目录与本地规则匹配，不是大模型动态生成人物。Figma无导出关键帧，过渡根据用户明确描述编写，不声称还原了不可读取的Smart Animate参数。

## 2026-09-15 星球与过渡光晕颜色联动

- 将固定黄色光晕改为原始 SVG 的 alpha 蒙版，以所选角色星球颜色着色，保留原有柔边及转场时间线。
- `npm run build`、`test:community` 通过；新增黄、粉、青、蓝四种颜色回归，检查星点、放大源与中间光晕颜色一致。蓝色和粉色截图已目视核对；无真实 API 调用。

## 2026-09-15 新手流程与自建伙伴更新

- 根据 Figma 15/16/17/20 与更新后的登录、想法气泡实现；读取用户补入的猫咪透明原图，沿用本地资源。欢迎页按新稿改为白底猫咪，不再使用旧风景光晕。站酷文艺体已本地打包。
- 新增自建角色前后端持久化、校验与服务器人设映射；仅备注/人物设定必填。原有数据无引导标记时保留老用户流程；未清空真实用户数据。
- `npm test`：15 通过，1 跳过（检测到真实 API 配置，为避免费用跳过演示响应测试）。自建角色的重启恢复、非法字段、重复 ID、头像隐私单元测试通过。
- `test:ui`、`test:community`、构建与 `test:production` 通过。社区测试适配持续旋转星球，以键盘激活移动星点；保留并验证不同星点的过渡光晕颜色。
- `test:companion` 基础流程通过：首登无底栏选角、必填校验、创建保存→Note→同一资料→自建角色演示聊天、云朵展开、置顶刷新、软删除并恢复、发现默认展开、键盘与减少动态、320/375/402/430/768 宽度。
- 补充异常路径回归通过：头像上传落盘；模拟第一次 PUT 失败时表单保留且不展示成功卡片，重试后仅有一个角色；触屏长按打开菜单；发现列表滚动后再次点击当前 Tab，星球展开且滚动归零。
- `test:hmr` 最终复测通过：CSS 和 React 热更新保留当前聊天与输入草稿。
- 目视检查 welcome/onboarding/create/created-note/custom-profile/cloud-open 截图，修正透明插画灰底与 Note 头像无约束尺寸问题。
- 本次没有新增真实 DeepSeek 调用；自建角色复用现有真实 API 通路，但新角色的模型实际表现未付费验证。社区仍为本地单用户预览。

## 2026-09-16 交互细节回归

- 删除聊天弹窗补齐正文和玻璃按钮内边距，调整标题及说明文字层级；截图检查无贴边。
- 星球进入页面立即自动旋转，测试取非极点星星验证无需触摸的位置变化；动态响应减少动画设置。
- 收起云朵增大字号、柔化轮廓；展开与收起均通过背景层轻微漂浮，文字及点击区域保持稳定。首页猫咪和 Hi 添加微动效。
- 自建角色资料页显示铅笔编辑入口，保存后返回同一角色资料页；创建与编辑分开处理，创建失败重试不会误入编辑流程。
- 按最新反馈，欢迎页的微信按钮每次都进入 Life gift；已登录刷新仍恢复聊天列表。登录仍为本地模拟微信入口。
- 验证：构建通过；6 项存储/持久化测试通过；使用本机 Chrome 的 `test:companion` 通过，覆盖创建失败重试、资料编辑、聊天、云朵点击、删除与恢复、未触摸自动旋转、再次登录/刷新、窄屏及减少动画。
- 本轮未调用真实 DeepSeek API。

### 同日按最新设计反馈修正

- 睡眠 Z 使用 Figma `588:34478` 的三张手绘 SVG 原件，保存为 `public/assets/welcome-sleep-z-*.svg`；按原稿恢复大/中/小尺寸、深浅和位置，插画顶边同步为 117/874。循环仅轻微上飘、淡出，减少动态效果显示完整静态排列。

## 2026-09-16 Figma 22–41 视觉更新

- 已完成：白底/浅灰气泡、浅绿玻璃边线、图41胶囊聚焦边线、浅蓝云朵原件、标题/分隔线/标签/资料/记忆/评论弹窗和 Note 比例调整；保留已有猫咪、Hi、睡眠Z、星球自动旋转及颜色联动。
- `npm run build`、`node --check tests/visual.mjs`、`git diff --check` 通过；`npm test` 最近一次在本机 Chrome 环境通过（15通过、1项因真实 API 配置跳过）。
- 新增 `npm run test:visual` 作为隔离视觉回归入口。当前执行环境的浏览器回归再次运行受本机 Codex 使用额度限制，未将未完成的截图回归写成通过；解除限制后按 README 命令运行即可生成 `test-results/v4-*.png`。
- 验证：构建通过，Chrome 检查三个资源加载和循环动画，静态截图对照 Figma 位置；减少动态效果时动画为 none。

- 收起云朵主体恢复 36px 高、16px 字号，移除叠加的上下留白；展开态沿用原内边距。轮廓仍轻微漂浮。
- Hi 使用原稿六个 SVG 笔画和逐笔遮罩，约 3 秒写完后停留，完成状态目视核对；减少动态效果时直接显示完整字样。
- 欢迎页微信按钮每次固定进入 Life gift；测试已改为验证回到欢迎页后再次点击也进入引导。

## 2026-09-17 聊天体验 P0（人设卡 / 时间感知 / 记忆挑选）

- 新增 `shared/memory.mjs`（记忆规范化、去重合并、打分挑选、降噪）、`shared/persona-cards.mjs`（8个内置角色的人设卡）、`shared/context.mjs`（当前时间、距上次对话、深夜/午饭提示）、`shared/prompt.mjs`（system 组装，稳定块在前、易变时间在末尾以吃到 DeepSeek 前缀缓存）。
- 模型现在回传结构化记忆 `{text, kind, salience, emotion}`；重复事实合并计数不再堆叠；进入模型的记忆由打分挑选（稳定事实保底 4 条），图片条目与演示摘录不占槽位；被实际用上的记忆会加权（hits）。`server/state-store.mjs` 相应放宽并校验新字段。
- `tests/memory.test.mjs` 11 项通过（去重、降噪、40天前的稳定事实仍入选、query 相关记忆靠前、敏感信息拒绝、prompt 块顺序、新字段通过状态校验）。
- 本次实际运行：`npm run build` 通过；单测共 27 项——26 通过、1 项因本机已配置真实密钥而跳过（避免付费）；`npm run test:ui` 通过（使用系统 Chrome：`PLAYWRIGHT_CHROME_PATH='/Applications/Google Chrome.app/Contents/MacOS/Google Chrome'`）。
- 真实 API 验证 1 次（deepseek-flash）：返回 3 条短气泡「嗯。桂花乌龙。/ 这个点……吃了没？/ 累了就坐会儿，别站着说话。」，旁白与结构化记忆均正常：
  `{"text":"最喜欢喝桂花乌龙","kind":"preference","salience":4,"emotion":"温和"}`。
- 尚未验证：多轮长对话后挑选策略的实际体感与 token 成本变化、跨角色记忆共享、情绪化效果的主观评价；这些属于 P1/P2 范围。

## 2026-09-17 记忆与时间逻辑修复回归

- 修正发送后时间间隔为零、入选记忆全部累加 hits、旧版聊天摘录混入上下文、30天半衰公式与描述不符、自定义关系被固定为刚认识等问题。重复确认与引用分为 mentionCount / referenceCount；同义事实支持保守规则和受限 matchId，不跨日期自动合并事件。
- 深夜和午饭提示扫描完整历史识别已提醒状态，跨午夜按同一晚处理；关键词识别存在覆盖边界，并非绝对保证模型不重复。
- 实际验证：26项离线单元测试通过（memory/store/persistence/custom-characters/community），`npm run build` 通过。
- `PLAYWRIGHT_CHROME_PATH='/Applications/Google Chrome.app/Contents/MacOS/Google Chrome' node tests/run-isolated.mjs --memory` 通过：浏览器真实点击发送 → 检查请求间隔/摘录过滤 → 模拟模型返回有效/重复/非法引用ID → 验证仅被引用事实加权、重复事实合并及刷新持久化。使用临时数据，没有修改用户聊天。
- 本轮未调用真实 DeepSeek，未执行全部UI套件；真实模型的语义合并准确性、引用自报准确性、长期多轮体感尚需验证。以上不替代历史测试记录，也不把模拟响应视为真实API通过。

## 尚未验证 / 明确边界（历史记录）

- 2026-09-15 用户报告偶发「连接或回复格式异常」：单独表情真实请求复测HTTP200、3段回复，约1.5秒，未复现原故障。当前服务把网络异常与JSON解析/字段校验失败合并提示，历史无对应诊断信息，不能断定那次根因；本次仅修正输入框焦点样式，没有声称修复接口偶发问题。

- 已通过真实API验证，但不能保证后续余额、服务可用性或网络；错误会显示重试。服务重启后UI重新待验证，首次成功请求才显示已连接。
- 微信登录为明确标注的体验入口，无真实 OAuth。
- 语音输入依赖浏览器 SpeechRecognition 和用户麦克风授权，未进行真人语音测试。
- 未做原生 iOS / Android 安装包、真机软键盘全机型测试或生产安全审计。
- 演示模式的后续回复是本地规则，记忆是摘录，不是 AI 理解；示例日常与初始回忆不是用户真实数据。
- 单机单用户后端文件存储，不是已上线的多用户产品；无公网鉴权、生产数据库、图片对象存储和跨设备账号体系。未发布公网。

后续修改可先 `npm run build`，启动服务后 `npm test`、`npm run test:ui`、`npm run test:hmr`、`npm run test:production`。按需付费验证用 `npm run test:live`。详见 README。
